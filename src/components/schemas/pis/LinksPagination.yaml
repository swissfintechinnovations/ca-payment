title: Pagination Links
description: Links (or cursors) returned in the answer of an API call.
type: object
properties:
  self:
    type: string
    description: >-
      Link / cursor to the current result set (based on the submitted pagination
      approach).
    example: /payment-app/api/v1/payments?offset=75&limit=25
  first:
    type: string
    description: >-
      Link / cursor to the first result set (based on the submitted pagination
      approach).
    example: /payment-app/api/v1/payments?offset=0&limit=25
  previous:
    type: string
    description: >-
      Link / cursor to the previous result set (based on the submitted
      pagination approach).
    example: /payment-app/api/v1/payments?offset=50&limit=25
  next:
    type: string
    description: >-
      Link / cursor to the next result set (based on the submitted pagination
      approach).
    example: /payment-app/api/v1/payments?offset=100&limit=25
  last:
    type: string
    description: >-
      Link / cursor to the last result set (based on the submitted pagination
      approach).
    example: /payment-app/api/v1/payments?offset=150&limit=25
