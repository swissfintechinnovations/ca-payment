description: |
  This element shall always be populated with the account alias, if defined, otherwise with specific bank account name (e.g. bank saving
  account). This attribute is required in the Account API, while optional in SPS. Reason: Necessary for the multibanking use case. Ref: Issue#98.
type: string
maxLength: 140
example: Firmenkonto
