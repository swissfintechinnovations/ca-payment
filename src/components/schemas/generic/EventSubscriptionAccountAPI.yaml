EventSubscriptionAccountAPI:
  type: object
  required:
    - eventTypes
  properties:
    eventSubscriptionId:
      $ref: '#/components/schemas/EventSubscriptionId'
    eventTypes:
      type: array
      items:
        type: string
        enum:
          - accounts:listUpdated
      example:
        - accounts:listUpdated
    callbackUrl:
      type: string
      example: https://ttp.com/API/notifications/account-information-service/
      description: Not to be used in bLink
